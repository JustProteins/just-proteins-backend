export enum Namespace {
  APP = 'app',
  REDIS = 'redis',
  POSTGRES = 'postgres',
  PGADMIN = 'pgadmin'
}

export enum Environment {
  DEVELOPMENT = 'development',
  STAGING = 'staging',
  PRODUCTION = 'production',
}
